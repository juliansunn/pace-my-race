import React from 'react';
import { CoachForm } from '../../../../components/CoachApply';

type Props = {};

const CoachSettings = (props: Props) => {
	return (
		<div>
			<CoachForm />
		</div>
	);
};

export default CoachSettings;
